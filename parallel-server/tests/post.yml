---

concurrency: 100
base: 'http://localhost:8081'
iterations: 1000000
rampup: 2

plan:
  - name: Post message
    request:
      headers:
        Authorization: "Bearer a"
      body:  '[{"deviceId": "b", "payload": { "cType": 0, "ciphertext": "hello"}}]'
      url: /message
